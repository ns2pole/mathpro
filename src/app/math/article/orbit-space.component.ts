import { Component } from '@angular/core';
import { Router } from '@angular/router';

@Component({
  selector: 'app-contact',
  templateUrl: './orbit-space.component.html',
})
export class OrbitSpaceComponent {
  constructor(private router: Router) {}
}

